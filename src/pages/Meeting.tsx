import React, { useEffect } from 'react';

export default function MeetingPage() {
    // const globalContext = useGlobalContext();

    // console.log(globalContext?.demo);

    // useEffect(() => {
    //     globalContext?.setDemo('asd');
    // }, []);

    return (
        <>
            {/* <GlobalProvider>
                <></>
            </GlobalProvider> */}
        </>
    );
}
